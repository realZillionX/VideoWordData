# VideoWordData Dependencies

# Core dependencies (existing)
datasets
opencv-python
numpy
pillow
tqdm
tiktoken

# Audio video generation (offline TTS)
piper-tts  # Piper TTS - offline, supports Python 3.12
moviepy
onnxruntime-gpu  # GPU acceleration for Piper TTS
