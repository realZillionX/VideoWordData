# VideoWordData Dependencies

# Core dependencies (existing)
datasets
opencv-python
numpy
pillow
tqdm
tiktoken

# Audio video generation (offline TTS)
TTS  # Coqui TTS - offline, GPU-accelerated
moviepy
